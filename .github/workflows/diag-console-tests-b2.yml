name: Diagnostic Console tests B2

on:
  pull_request:
    branches:
      - feat/auth-gated-console-20260825

permissions:
  contents: read

jobs:
  tests-b2:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: node --test test/stripe-billing-console.test.js test/vercel-preview-adapter.test.js
