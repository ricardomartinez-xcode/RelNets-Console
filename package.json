{
  "name": "@relead/app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Authenticated ReLead user console for RelNet.",
  "scripts": {
    "test": "node --test",
    "check": "npm test && npm run build:worker",
    "deploy": "wrangler deploy",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "build:worker": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "eslint": "^9.39.5",
    "typescript": "^6.0.2",
    "wrangler": "4.30.0"
  }
}
