import test from'node:test';import assert from'node:assert/strict';import{injectTheme,isPanelPath,isThemeableStylesheet,normalizeOrigin,shouldInjectHtml}from'../src/policy.js';
test('only Admin and Console are proxied',()=>{assert.equal(isPanelPath('/admin'),true);assert.equal(isPanelPath('/admin/api/session'),true);assert.equal(isPanelPath('/console/share-target'),true);assert.equal(isPanelPath('/mcp'),false);assert.equal(isPanelPath('/administer'),false)});
test('stylesheet override is scoped',()=>{assert.equal(isThemeableStylesheet('/admin/static/styles.css'),true);assert.equal(isThemeableStylesheet('/console/static/styles.css'),true);assert.equal(isThemeableStylesheet('/admin/static/app.js'),false)});
test('theme injection is idempotent',()=>{const h='<html><head></head><body></body></html>',o=injectTheme(h);assert.match(o,/theme\.css/);assert.match(o,/theme-client\.js/);assert.equal(injectTheme(o),o)});
test('HTML detection and origin hardening',()=>{assert.equal(shouldInjectHtml('text/html; charset=utf-8'),true);assert.equal(shouldInjectHtml('application/json'),false);assert.equal(normalizeOrigin('https://api.relead.com.mx/x'),'https://api.relead.com.mx');assert.throws(()=>normalizeOrigin('http://api.relead.com.mx'),/HTTPS/)});
